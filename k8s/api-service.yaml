apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  type: LoadBalancer # For public access from the frontend browser
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: api
